[defaults]
inventory = inventory/hosts.ini
remote_user = ansible
private_key_file = ~/.ssh/id_ed25519_k8s_homelab
host_key_checking = False
retry_files_enabled = False
stdout_callback = default

[privilege_escalation]
become = True
become_method = sudo

[callback_default]
result_format = yaml
