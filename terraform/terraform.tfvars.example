# Copy to terraform.tfvars and fill in real values. Never commit terraform.tfvars.
proxmox_api_token = "terraform@pve!k8s-homelab=REPLACE_WITH_TOKEN_SECRET"
ssh_public_key    = "ssh-ed25519 AAAA... k8s-homelab-ansible"
